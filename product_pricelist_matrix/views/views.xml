<odoo>
    <data>
        <record id="action_product_template_price_list_matrix" model="ir.actions.server">
            <field name="name">Generate Pricelist Matrix</field>
            <field name="groups_id" eval="[(4, ref('product.group_product_pricelist'))]"/>
            <field name="model_id" ref="product.model_product_template"/>
            <field name="binding_model_id" ref="product.model_product_template"/>
            <field name="state">code</field>
            <field name="code">
ctx = env.context
action = {
'name': 'Pricelist Matrix',
'type': 'ir.actions.client',
'tag': 'generate_pricelist_matrix',
'context': ctx,
}
            </field>
        </record>

        <record id="action_product_product_price_list_matrix" model="ir.actions.server">
            <field name="name">Generate Pricelist Matrix</field>
            <field name="groups_id" eval="[(4, ref('product.group_product_pricelist'))]"/>
            <field name="model_id" ref="product.model_product_product"/>
            <field name="binding_model_id" ref="product.model_product_product"/>
            <field name="state">code</field>
            <field name="code">
ctx = env.context
action = {
'name': 'Pricelist Matrix',
'type': 'ir.actions.client',
'tag': 'generate_pricelist_matrix',
'context': ctx,
}
            </field>
        </record>


    </data>
</odoo>